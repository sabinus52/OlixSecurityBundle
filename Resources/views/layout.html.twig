<!DOCTYPE html>
<html>
<head>
<meta charset="utf-8">
<meta http-equiv="X-UA-Compatible" content="IE=edge">
<meta name="viewport" content="width=device-width, initial-scale=1">
{% include "OlixAdminBundle::head.html.twig" %}
<title>{% block title %}BO - {{ olix.brand }}{% endblock %}</title>
{% include "OlixAdminBundle::stylesheet.html.twig" %}
<link href="{{ asset('bundles/olixsecurity/css/style.css') }}" rel="stylesheet">
{% if olix.theme %}<link href="{{ asset(olix.theme) }}" rel="stylesheet">{% endif %}
{% block stylesheet %}{% endblock %}
</head>

<body class="olix-login">

<div class="navbar navbar-inverse navbar-fixed-top" role="navigation">
    <div class="container-fluid">
        <div class="navbar-header">
            <a class="navbar-brand" href="#">{% if olix.logo is defined %}<img alt="" src="{{ asset(olix.logo) }}">{% endif %} {{ olix.brand }}</a>
            <p class="navbar-text hidden-xs hidden-sm">{{ olix.description|raw }}</p>
        </div>
    </div>
</div>

<div class="container">
    <div class="row"  style="margin-top: 80px;">
        <div class="col-md-4 col-md-offset-4">
        {% for type, messages in app.session.flashBag.all %}
            {% for message in messages %}
                <div class="{{ type }}">
                    {{ message|trans({}, 'FOSUserBundle') }}
                </div>
            {% endfor %}
        {% endfor %}
        {% block fos_user_content %}{% endblock fos_user_content %}
        </div>
    </div>
</div>

{% include "OlixAdminBundle::javascript.html.twig" %}
{% block javascript %}{% endblock %}
</body>
</html>
